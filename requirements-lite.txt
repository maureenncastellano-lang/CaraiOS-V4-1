# CaraiOS v2 — Lite Requirements (Termux / low-power devices)
# Removes: chromadb, numpy, heavy ML deps
# Memory falls back to: SQLite (built-in, zero deps)

# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.0
starlette==0.37.2

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0

# Auth + Crypto
bcrypt==4.1.3
pyjwt==2.8.0
cryptography==42.0.7
python-multipart==0.0.9

# HTTP
httpx==0.27.0

# Scheduling
apscheduler==3.10.4

# Templating
jinja2==3.1.4
python-dotenv==1.0.1

# Notifications (optional)
aiosmtplib==3.0.1

# NOTE: chromadb and supabase are optional
# Memory will use SQLite fallback automatically
# Install these only if you want vector search:
#   pip install chromadb
#   pip install supabase
